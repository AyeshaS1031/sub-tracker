name: sub_tracker
description: "New Flutter project- A simple app to track your subscriptions and manage them effectively."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.0
  hive_flutter: ^1.1.0
  fl_chart: ^0.69.2
  intl: ^0.20.2
  flutter_local_notifications: ^19.0.0
  timezone: ^0.10.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

